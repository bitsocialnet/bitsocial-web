global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: bitsocial-stats-monitor
    metrics_path: /metrics/prometheus
    relabel_configs:
      - target_label: instance
        replacement: bitsocial-stats-monitor
    static_configs:
      - targets:
          - monitor:3000
